psutil>=5.9
pyyaml>=6.0
python-telegram-bot>=21.0
nvidia-ml-py>=12.0          # pynvml — NVIDIA GPU metrics
pythonnet>=3.0.0            # .NET interop for LibreHardwareMonitor DLL
pywin32>=306                # Win32 API for idle time + session detection
